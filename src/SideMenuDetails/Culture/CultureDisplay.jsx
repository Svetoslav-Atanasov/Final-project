import React from "react";
import SingleOfferTemp from "../../OfferDisplaySingleTemplate/OfferDisplaySingleTemplate";

const CultureDisplay = props => {
  return (
    <React.Fragment>
      {/* {props.allOffers.map(offer =>
        offer.description === "culture" ? (
          <SingleOfferTemp
            key={offer.id}
            name={offer.name}
            image={offer.image}
            category={offer.category}
            description={offer.description}
            price={offer.price}
          />
        ) : null
      )} */}
    </React.Fragment>
  );
};

export default CultureDisplay;
